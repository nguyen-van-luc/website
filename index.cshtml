@model IEnumerable<Doanweb.Models.TbBlog>

@{
    ViewData["Title"] = "Quản lý Blog";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">

    <section class="content-header">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            <div>
                <h2 class="fw-bold">Quản lý Blog</h2>
                <p class="text-muted mb-0">Danh sách bài viết</p>
            </div>

            <a asp-action="Create" class="btn btn-success px-4">
                ➕ Thêm blog
            </a>
        </div>
    </section>

    <section class="content">
        <div class="container-fluid">

            <div class="card shadow border-0 rounded-4">
                <div class="card-body p-0">

                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>#</th>
                                <th>Tiêu đề</th>
                                <th>Danh mục</th>
                                <th>Người viết</th>
                                <th>Trạng thái</th>
                                <th class="text-end">Hành động</th>
                            </tr>
                        </thead>

                        <tbody>
                            @{
                                int i = 1;
                            }
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@(i++)</td>
                                    <td class="fw-semibold">@item.Title</td>
                                    <td>@item.Category?.Title</td>
                                    <td>@item.Account?.FullName</td>
                                    <td>
                                        @if (item.IsActive == true)
                                        {
                                            <span class="badge bg-success">Hiển thị</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Ẩn</span>
                                        }
                                    </td>
                                    <td class="text-end">
                                        <a asp-action="Edit"
                                           asp-route-id="@item.BlogId"
                                           class="btn btn-sm btn-outline-primary mx-1">✏️</a>

                                        <a asp-action="Delete"
                                           asp-route-id="@item.BlogId"
                                           class="btn btn-sm btn-outline-danger"
                                           onclick="return confirm('Xóa blog này?')">🗑️</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>

        </div>
    </section>
</div>
